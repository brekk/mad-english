import IO from "IO"

import { Regex, UnflaggedRegex } from "./Regex"
import { stemmer } from "./Stemmer"



main = () => {
  IO.putLine("Hello World!")
  pipe(
    UnflaggedRegex,
    IO.trace("reggie watts"),
  )("[^aeiou]")
  pipe(
    stemmer,
    IO.trace("stemstimstomstam"),
  )("issues")
}
